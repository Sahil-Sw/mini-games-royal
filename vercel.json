{
  "buildCommand": "npm install && npm run build:shared && npm run build:client",
  "outputDirectory": "client/dist",
  "framework": null,
  "installCommand": "npm install --workspaces",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

